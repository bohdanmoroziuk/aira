import { defineAppConfig } from '#imports'

export default defineAppConfig({
  title: 'Aira',
  ui: {
    colors: {
      // Aira — "air / aura": a calm, intelligent AI assistant. Violet reads
      // premium and creative and stands apart from the generic-blue chat UIs,
      // while staying legible in both light and dark mode.
      primary: 'violet',
      neutral: 'slate',
    },
  },
})
